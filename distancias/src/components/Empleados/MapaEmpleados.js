import React from 'react';
import PropTypes from 'prop-types';

const MapaEmpleados = props => {
    return (
        <div>
            {/* MapaEmpleados */}
        </div>
    );
};

MapaEmpleados.propTypes = {
    
};

export default MapaEmpleados;