import React from 'react';
import PropTypes from 'prop-types';

const Barra = props => {
    return (
        <div>
            
        </div>
    );
};

Barra.propTypes = {
    
};

export default Barra;