import React from 'react';
import PropTypes from 'prop-types';

const Menu = props => {
    return (
        <div>
            <MenuBarra/>
            
        </div>
    );
};

Menu.propTypes = {
    
};

export default Menu;